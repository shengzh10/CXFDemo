server:
  port: 8081


spring:
  # Data Source
  datasource:
    url: jdbc:mysql://localhost:3306/supermarket
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  #thymeleaf
  thymeleaf:
    cache: false
    mode: HTML5

# Mybatis
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.distribute.demo.model

#debug: true
